const BackButton = ({onClick}) => (
    <button 
    onClick={onClick}
    className="w-full font-semibold bg-slate-300 hover:bg-slate-400 p-2"   
    >
        Back
    </button>
)

export default BackButton;